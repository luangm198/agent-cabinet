# This app does NOT require an Anthropic API key — it uses your Claude Code
# login (claude.exe) through your Claude Max subscription.
#
# A .env file is OPTIONAL; normally you don't need one at all.
# Advanced: to force a specific claude.exe path, copy this file to .env and set:
# CLAUDE_CLI=C:\path\to\claude.exe
